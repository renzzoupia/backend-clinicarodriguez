# Configuración para producción usando variables de entorno

spring.application.name=clinicarodriguez

# Base de datos - usar variables de entorno
spring.datasource.url=${DATABASE_URL:jdbc:mysql://hopper.proxy.rlwy.net:54881/railway}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:bqzullTNaujzuCmFibynLCceaDtWibuq}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# JWT
jwt.secret=${JWT_SECRET:ClaveSecretaClinicaRodriguezGrupoPerfilDeEgreso5toanio123456789}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Almacenamiento de archivos
file.upload-dir=${UPLOAD_DIR:./uploads}

# Multipart
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Puerto del servidor
server.port=${PORT:8080}

# Logging
logging.level.root=INFO
logging.level.com.clinicarodriguez=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Compresión de respuestas
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css

# CORS - permitir todos los orígenes en producción (ajustar según necesidad)
# Ya está configurado en CorsConfig.java
